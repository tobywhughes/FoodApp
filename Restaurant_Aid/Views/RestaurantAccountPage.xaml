<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:control="clr-namespace:Restaurant_Aid.Controls"
    prism:ViewModelLocator.AutowireViewModel="True"
    x:Class="Restaurant_Aid.Views.RestaurantAccountPage"
    x:Name="RestaurantAccount">

    <Grid x:Grid.Column="1"
          ColumnSpacing="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

    
    <!-- <control:InputControl x:Grid.Column="0"
                              x:Grid.ColumnSpan="2"
                              x:Grid.Row="0"
                              Caption="RestaurantName:"
                              Value="{Binding RestuarantAccountPageViewModel.Name, Mode=TwoWay}" 
                              />
    -->

    </Grid>
 

</ContentPage>